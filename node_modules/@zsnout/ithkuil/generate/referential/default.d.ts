import type { PartialReferential, Referential, ReferentialReferentialCore, SuppletiveReferentialCore } from "./referential.js";
/** The default referential (1m:NEU, M, THM, NRM). */
export declare const DEFAULT_REFERENTIAL: ReferentialReferentialCore;
/** The default suppletive referential (CAR, THM, NRM). */
export declare const DEFAULT_SUPPLETIVE_REFERENTIAL: SuppletiveReferentialCore;
/**
 * Fills default values into empty slots of a referential/
 *
 * @param referential The referential to be filled.
 * @returns A complete referential, with all default slots filled.
 */
export declare function fillInDefaultReferentialSlots(referential: PartialReferential): Referential;
