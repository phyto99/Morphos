/**
 * Clones a node.
 *
 * @param props Properties modifying the cloned element.
 * @returns The cloned node.
 */
export declare function Clone<T extends Node>(props: {
    /** The node to be cloned. */
    readonly children: T;
}): T;
