import { jsx as _jsx } from "@zsnout/ithkuil-jsx/jsx-runtime";
import { Translate } from "./translate.js";
/**
 * Shows a spread of several items.
 *
 * @param props Properties modifying the spread.
 * @returns An `SVGGElement` containing all the items.
 */
export function Spread(props) {
    const g = (_jsx("g", {}));
    const columns = props.columns ?? 5;
    for (let index = 0; index < props.items.length; index++) {
        g.append(_jsx(Translate, { x: (index % columns) * (props.x ?? 100), y: (index / columns) * (props.y ?? 100), children: props.items[index] }));
    }
    return g;
}
