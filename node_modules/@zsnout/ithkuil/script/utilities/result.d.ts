export type Result<T> = {
    readonly ok: true;
    readonly value: T;
} | {
    readonly ok: false;
    readonly reason: string;
};
export declare function HandleResult<T, U, V>(props: {
    children: Result<T>;
    ok(value: T): U;
    error(reason: string): V;
}): U | V;
